package delta.common.utils.graph;

import java.awt.Dimension;

/**
 * Default/standard values for graph-related entities.
 * @author DAM
 */
public class GraphConstants
{
  /**
   * Standard size for a node.
   */
  public static final int STD_SIZE=50;
  /**
   * Standard dimension for a node. It uses the standard size vor both the
   * horizontal and the vertical value.
   */
  public static final Dimension STD_DIMENSION=new Dimension(STD_SIZE,STD_SIZE);
}
